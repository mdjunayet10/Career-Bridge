[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run prisma:generate"

[deploy]
startCommand = "npm start"
preDeployCommand = "npm run prisma:migrate && npm run prisma:seed"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
